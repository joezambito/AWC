import SwiftUI

// MARK: - WealthTheme
//
// App-wide color constants.
// The real token values live in the Xcode asset catalog / design system.
// These stubs use close approximations so layouts compile and render.

enum WealthTheme {
    static let cyan   = Color(red: 0.00, green: 0.85, blue: 0.90)
    static let blue   = Color(red: 0.10, green: 0.45, blue: 0.95)
    static let orange = Color(red: 1.00, green: 0.62, blue: 0.15)
    static let green  = Color(red: 0.18, green: 0.80, blue: 0.44)
    static let red    = Color(red: 0.95, green: 0.25, blue: 0.25)
}
